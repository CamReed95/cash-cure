import React, {Component} from 'react';
import {render, fireEvent} from '@testing-library/react';
import Counter from '../Components/Income';

describe('testing for the Income component', () => {

    it('Renders out starting text', () => {
        const {}
    })

})